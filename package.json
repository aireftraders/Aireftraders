{
  "name": "aireftraders",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "clean": "rd /s /q node_modules && del package-lock.json",
    "deploy": "vercel deploy --prod --regions iad1",
    "full-deploy": "npm run clean && npm install && npm run deploy"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "crypto-js": "^4.1.1",
    "@vercel/node": "^3.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}